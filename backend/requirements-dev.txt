# Development & test dependencies — NOT installed in production (PRE-013).
# Production installs requirements.txt only (see render.yaml). CI/local installs
# both:   pip install -r requirements-dev.txt
-r requirements.txt

# Test framework
pytest==8.3.0
pytest-asyncio==0.24.0
pytest-cov==6.0.0
pytest-timeout
factory-boy==3.3.0
faker==30.0.0
aiosqlite
freezegun
pyyaml

# Linters / formatters / type-checkers
mypy==1.13.0
black==24.10.0
isort==5.13.0
flake8==7.1.0
